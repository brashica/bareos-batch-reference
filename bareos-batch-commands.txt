## Run a list of jobs
for job in $(cat joblist.txt); do echo -e "run job=$job level=Incremental\nyes" | bconsole; done

## Get list of configured jobs
echo "show jobs" | bconsole  | grep -E "vast" | awk '{print $3}'  | uniq  | tr -d '"'
# with matching name/sufffix/prefix
echo "show jobs" | bconsole  | grep -E "name/suffix/prefix" | awk '{print $3}'  | uniq  | tr -d '"'
